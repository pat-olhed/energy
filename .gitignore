# envs
.venv/
__pycache__/
*.py[cod]

# notebooks
.ipynb_checkpoints/

# data (fetched via src/data.py; keep folders, ignore contents)
data/raw/*
data/processed/*
!data/**/.gitkeep
# keep the small backtest artifacts so the hosted app runs without a rebuild
!data/processed/backtest_results.parquet
!data/processed/backtest_metrics.csv

# models / artifacts
*.pkl
*.joblib
models/

# secrets & OS
.env
.DS_Store

# private working brief — not part of the portfolio
CLAUDE.md
