# envs
.venv/
__pycache__/
*.py[cod]

# notebooks
.ipynb_checkpoints/

# data (fetched via src/data.py; keep folders, ignore contents)
data/raw/*
data/processed/*
!data/**/.gitkeep
# keep the small price-backtest artifacts so the hosted app runs without a rebuild
!data/processed/price_backtest_results.parquet
!data/processed/price_backtest_metrics.csv
!data/processed/price_backtest_regime.csv

# models / artifacts
*.pkl
*.joblib
models/

# secrets & OS
.env
.DS_Store

# private working brief — not part of the portfolio
CLAUDE.md

# internal working docs (plan + research notes) — not part of the portfolio
PLAN.md
research/
